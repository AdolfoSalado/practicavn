<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/background_date" /> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@color/background_date" /> <!-- Color del borde -->
    <corners android:radius="8dp" /> <!-- Radio de las esquinas -->
    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp" />
</shape>